# WordPress環境構築用環境変数設定例
# このファイルをコピーして .env ファイルを作成し、実際の値を設定してください

# データベース設定
WORDPRESS_DB_HOST=localhost
WORDPRESS_DB_PASSWORD=your_secure_password

# WordPress設定
WORDPRESS_DOMAIN=example.com

# SSH鍵設定
SSH_PRIVATE_KEY_PATH=/path/to/your/private_key.pem
SSH_PUBLIC_KEY_PATH=/path/to/your/public_key.pub

# 使用方法:
# 1. このファイルをコピー: cp ansible/example.env .env
# 2. 実際の値を設定
# 3. 環境変数を読み込み: source .env
# 4. Ansibleプレイブックを実行: ansible-playbook ansible/playbooks/wordpress_setup.yml

# データベース管理者設定（データベースユーザー作成用）
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=your-secure-admin-password
